package com.serenitask.controller;

public class EventLoader {

    // take n calendars in a list<Calendar>

    // Read in all entries in database events

    // Split into n lists based on amount of unique calendars, they have unique names

    // Load each item in each list into entries

    // calendar.addEntry ?

    // return 3 calendars

}
